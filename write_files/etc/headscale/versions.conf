# Headscale VPS Version Configuration
# Pinned to specific versions for security and reproducibility
# Update these values carefully and test before deploying to production

# NVM (Node Version Manager)
# Version from: https://github.com/nvm-sh/nvm/releases
NVM_VERSION="v0.40.1"
NVM_SHA256="abdb525ee9f5b48b34d8ed9fc67c6013fb0f659712e401ecd88ab989b3af8f53"

# Node.js version for Headplane (via nvm)
# v25.2.1+ has stable node:sqlite support (no --experimental-sqlite flag needed)
# Format: Major.Minor.Patch (e.g., "25.2.1")
NODE_VERSION="25.2.1"

# Node.js SHA256 checksum (for linux-x64)
# Verify at: https://nodejs.org/dist/v${NODE_VERSION}/SHASUMS256.txt
NODE_SHA256="b9f6a97e81c89a9df45526b4f86dafdccaf12b82295f7bf35bdb2b0f5e68744f"

# Headplane version (git tag from https://github.com/tale/headplane/tags)
# Format: "vX.Y.Z" (e.g., "v0.6.1") or "main" for latest
HEADPLANE_VERSION="v0.6.1"

# Headscale version (from https://github.com/juanfont/headscale/releases)
# Format: "X.Y.Z" without 'v' prefix (e.g., "0.27.1") or empty for latest
HEADSCALE_VERSION="0.27.1"